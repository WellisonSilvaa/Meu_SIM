function clickMenu(){
    if(itens.style.opacity == 0){
    itens.style.opacity = 1;
    itens.style.top = '75px';
    
    }else{
        itens.style.opacity = 0;
        itens.style.top = '-600px';
    }
}